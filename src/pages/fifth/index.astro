---
import Layout from '../../layouts/Layout.astro'
import { fifthAdditionalNavigation } from '../../config/navigation'
import HeroSection from '../../components/HeroSection.astro'
import { getPackageConfig } from '../../config/packages'

const packageConfig = getPackageConfig('fifth')
---

<Layout
  title="@RANDSUM/FIFTH - D&D 5th Edition Dice Rolling"
  description="Specialized dice rolling utilities for D&D 5th Edition with advantage, disadvantage, and spell mechanics"
  additionalNavigationItems={fifthAdditionalNavigation}
>
  <HeroSection
    description="Specialized dice rolling utilities for D&D 5th Edition with advantage, disadvantage, ability scores, and spell mechanics."
    class="overflow-hidden"
    packageName={packageConfig?.name}
    packageColor={packageConfig?.color}
    gameWebsiteUrl={packageConfig?.gameWebsiteUrl}
    githubUrl={packageConfig?.githubUrl}
  />
</Layout>
